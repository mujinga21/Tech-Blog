<div class="row">
  <div class="col-auto">
    <h2>Title {{title}}</h2>
  </div>
</div>

{{!-- <div class="row mt-4">
  <div class="col-md-6">
    <h3>Enter a Title:</h3> --}}

    <form action="">
     <div class="form-outline mb-4">
    <input type="text" id="post-title"  name="post-title" class="form-control" />
    <label class="form-label" for="title"></label>
  </div> 

      <!-- Message input -->
  <div class="form-outline mb-4">
    <textarea class="form-control" id="post-content" name = "post-content" rows="4"></textarea>
    <label class="form-label" for="form4Example3">Content</label>
  </div>
      
 <!-- Submit button -->
  <button type="submit" class="btn btn-primary btn-block mb-4">post update</button>
   <!-- Submit button -->
  <button type="submit" class="btn btn-primary btn-block mb-4">Delete</button>

</form>

  </div>

  {{#if posts.length}}
  <div class="col-md-6 post-list">
    <h3>Current Posts:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/postasnpm/{{post.title}}">{{post.content}}</a></h4>
      </div>


      
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>




